import React, { memo } from "react";

//create commMainPageon hexagon layout conteiner
const RatesConteiner: React.ElementType = () => {
  //renderblock
  return (
    <div className="ratesConteiner">

    </div>
  );
};

export default memo(RatesConteiner);

import React, { memo } from "react";

//create common hexagon layout conteiner
const HexagonConteiner: React.ElementType = () => {
  //renderblock
  return (
    <div className="hexagonConteiner">

    </div>
  );
};

export default memo(HexagonConteiner);
